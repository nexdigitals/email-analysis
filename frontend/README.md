This is a minimal Vite + React scaffold. To run locally:

```powershell
cd 'C:\Users\User\Desktop\email analysis\frontend'
npm install
npm run dev
```

The frontend will call `http://127.0.0.1:5000/analyze` by default; change the URL in `src/App.jsx` if needed.
